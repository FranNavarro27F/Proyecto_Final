const { Router } = require("express");
const {} = require("../../controllers/Lenguajes");
const { Lenguajes } = require("../db");

const router = Router();

// -----------------------------------------------
