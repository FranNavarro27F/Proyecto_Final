const { Router } = require("express");
const {} = require("../../controllers/Contratos");
const { Contratos } = require("../db");

const router = Router();

// -----------------------------------------------
