const { Router } = require("express");
const {} = require("../../controllers/Usuarios");
const { Usuarios } = require("../db");

const router = Router();

// -----------------------------------------------
