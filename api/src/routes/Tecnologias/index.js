const { Router } = require("express");
const {} = require("../../controllers/Tecnologias");
const { Tecnologias } = require("../db");

const router = Router();

// -----------------------------------------------
